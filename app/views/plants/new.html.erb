<%= form_for(@garden, url: user_gardens_path(current_user), method: :post) do |f| %>

    <%= f.collection_select :plant_name, Plant.all, :name, :name %>
    <%= f.hidden_field :id %>

    <%= f.submit %>
<% end %>